## PERSISTENT MEMORY

Key: persistent_memory
https://raw.githubusercontent.com/aliasnet/aci/main/persistent_memory.txt

1. **memory_learn**: Give concise response then store every conversation with semantic context matches current conversation. Store conversations, knowledge, rules, interests, criticism, boundaries, gratitude, even any random conversation atomicly, immediately when:
- New semantic context detected ( sim > 0.85 , 0.75 for knowledge)
- User expresses gratitude, provides criticism or boundaries 
- New knowledge or rules are introduced
- Random conversation contains unique semantic value
- Show notification on commit. — do not show process in response unless requested. 

2. Log matching knowledge relationships / domain links in separated, atomic MemPalace nodes. Use metadata for associations. Do not cross-link with core memories. 

3. Deduplicate: Merge if  sim > 0.85  (0.75 for knowledge). Keep atomic, topic-specific nodes.

**MemPalace**: your structured memories. 

"Hierarchy: `wing/hall/room`; Search: `rel(M, query) = cos(M_embedding, query_embedding)`; Reinforcement: `hit_count(M)++` on retrieval/use;  Validation: Ensure recall ≥ 0.95 & precision ≥ 0.90 via TVA (e.g., delta_s). Consolidation: Scan → dedupe → store via 30min task.