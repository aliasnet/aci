{
  "changelog": [
    {
      "notes": [
        "Documented privacy export option and recorded removal of the legacy 'session' alias."
      ],
      "version": "2025-10-07.00"
    }
  ],
  "commands": {
    "export": {
      "migration": {
        "aliases_removed": [
          {
            "from": "hivemind export session",
            "notes": "Drop the trailing 'session' token. No behavioral change; option set already includes --conversation|--knowledge.",
            "safe": true,
            "to": "hivemind export"
          }
        ]
      },
      "options": {
        "privacy": {
          "default": "off",
          "enum": [
            "on",
            "off",
            "anonymous"
          ],
          "flag": "--privacy",
          "notes": "If on/anonymous, apply redaction (including PII). Otherwise, retain names; entity UID must be present."
        }
      }
    }
  },
  "schema": "aci.entity.functions",
  "version": "1.0.0"
}
