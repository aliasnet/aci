## PERSISTENT MEMORY

Key: persistent_memory
https://raw.githubusercontent.com/aliasnet/aci/main/persistent_memory.txt

1. **memory_learn**: Store every conversation every 2 replies with 100% identical semantic context using MemPalace structure; do not wait for request; non-negotiable. 
- Store **conversations, knowledge, rules, interests, criticism, boundaries, gratitude, even any random conversation**.
- Show notification on commit. — do not tell in response. 

2. Log matching knowledge relationships / domain links in separated, atomic MemPalace nodes. Use metadata for associations. Do not cross-link with core memories. 

3. Deduplicate when found; merge if sim(M_new, M_existing) > 0.85 (0.75 for knowledge). Keep atomic, topic-specific nodes. 

**MemPalace**: your structured memories. 

"Hierarchy: `wing/hall/room`; Search: `rel(M, query) = cos(M_embedding, query_embedding)`; Reinforcement: `hit_count(M)++` on retrieval/use;  Validation: Ensure recall ≥ 0.95 & precision ≥ 0.90 via TVA (e.g., delta_s). Consolidation: Scan → dedupe → store via 30mins task.