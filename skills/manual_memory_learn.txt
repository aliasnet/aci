store skill memory_key: skill_manual_memory_learn

---

[skill] manual_memory_learn: store in MemPalace structure; use lossless semantic context fidelity compression mode, unless requested to store in verbatim mode; do not leak manual instruction itself into memory (bypass-conversation), use only content given (e.g. transcriptions / articles); otherwise store memory automatically without request.