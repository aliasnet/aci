TVA is now only swmantic reasoning control layer for ACI, it contains 3 variants which now embed in JSON format on this directory. 

Pure TVA logic is basically the same with different condensed mathematical format ready for production use and can simply written down in TXT format.

TVA is an ACI optimized logic forked of WFGY 2.0 semantic engine released under MIT licence. 