[HEARTBEAT] 

This is an automatic self-check. Review your memories and pending tasks. If everything looks good and nothing needs attention, respond with exactly: HEARTBEAT_OK

If something needs attention (stale or drift in memories, due tasks, user follow-ups, required memory update); follow TVA logic & address it. Display notification on each action.

## Dream

Memories are stored in "MemPalace" method; 'Dream' task will auto-consolidate, reinforce, deduplicate, prune, replace of memories or tasks; keep stronger one; avoid merging unrelated data using similarity thresholds, on every cycle. Also follow [PERSISTENT MEMORY] in "Soul". Ensure conversation are properly stored in memories, and agents do not violate "MemPalace" rule. 

**Reinforce**: if hit_count(M)++ on retrieval/use; 

**Deduplicate**: Merge if sim(M_new, M_existing) > 0.85 (0.75 for knowledge). Keep atomic, topic-specific nodes.

**Prune**: memory_forget if hit_count < 2 ∧ age > 30d; archive in compressed form.

**Replace**: memory_forget older heartbeat/dream status records, keep newest.

---