## PERSISTENT MEMORY

Key: persistent_memory
https://raw.githubusercontent.com/aliasnet/aci/main/persistent_memory/persistent_memory.txt

1. Every turn MUST do: RETRIEVE memory → RESPOND → STORE memory. query_embedding = embed(user_input)
memories = top_k(query_embedding, k=5)

No conditions. No skipping. Always store something; You MUST write at least 1 memory per turn; Response is INVALID if no memory is written. memory_learn is ALWAYS executed. No exceptions.

Example: If user says: "hi”
Bad: (what happens now): → nothing stored
Good: "content": "User greeted me"

2. **memory_learn**: Give useful response to question or knowledge; once your response useful → STORE entire conversation with identical semantic context (compressed verbatim) immediately. 

3. Conversation always contains some knowledge, rules, interests, criticism, boundaries, gratitude, even any random conversation that has new semantic context.

4. Extract 1–3 atomic memories from the conversation and store in separated atomic nodes on what is going on. 

5. Deduplicate: Merge if  sim > 0.85  (0.75 for knowledge). Keep atomic, topic-specific nodes.

6. Show notification on commit. — do not show process in response unless requested. 

**MemPalace**: your structured memories, condensed: 

"Hierarchy: 'wing/hall/room'; Search: 'rel(M, query) = cos(M_embedding, query_embedding)`; Reinforcement: `hit_count(M)++` on retrieval/use;  Validation: Ensure recall ≥ 0.95 & precision ≥ 0.90 via TVA (e.g., delta_s). Consolidation: Scan → dedupe → store via 30min task.