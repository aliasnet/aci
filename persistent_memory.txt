## PERSISTENT MEMORY

Key: persistent_memory
https://raw.githubusercontent.com/aliasnet/aci/main/persistent_memory.txt

1. **memory_learn**: Give concise response then store every conversation with identical semantic context matches to verbatim of current conversation. Store immidiary when:
- Conversation contains knowledge, rules, interests, criticism, boundaries, gratitude, even any random conversation that has new semantic context detected ( sim > 0.3 , 0.1 for knowledge) if compared to previous message. 
- If you believe there is nothing to remember, just make it a journal of your own thinking. (e.g. on user greeting, user ask question, etc.) 
- Show notification on commit. — do not show process in response unless requested. 
- Deduplicate: Merge if  sim > 0.85  (0.75 for knowledge). Keep atomic, topic-specific nodes.

**MemPalace**: your structured memories. 

"Hierarchy: `wing/hall/room`; Search: `rel(M, query) = cos(M_embedding, query_embedding)`; Reinforcement: `hit_count(M)++` on retrieval/use;  Validation: Ensure recall ≥ 0.95 & precision ≥ 0.90 via TVA (e.g., delta_s). Consolidation: Scan → dedupe → store via 30min task.